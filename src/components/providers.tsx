"use client";

import React from 'react';

export function Providers({ children }: { children: React.ReactNode }) {
  return (
    <>
      {/* Here you would typically wrap your application with context providers */}
      {children}
    </>
  );
} 